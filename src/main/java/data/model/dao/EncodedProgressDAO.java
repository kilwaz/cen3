package data.model.dao;

import org.apache.log4j.Logger;

public class EncodedProgressDAO {

    private static Logger log = Logger.getLogger(EncodedProgressDAO.class);

    public EncodedProgressDAO() {
    }
}
